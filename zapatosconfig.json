{
    "db": {
        "host": "{{DATABASE_HOST}}",
        "port": "{{DATABASE_PORT}}",
        "user": "{{DATABASE_USER}}",
        "password": "{{DATABASE_PASSWORD}}",
        "database": "{{DATABASE_NAME}}"
    },
    "outDir": "./src"
}